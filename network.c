#include "network.h"
#include <stdio.h>

void InitNetwork(NetworkRole role, const char* ip) {
    printf("Red inicializada (Dummy)\n");
}

void SendPlayerState(Player* p) {
    // Pendiente
}

void ReceiveWorldState() {
    // Pendiente
}